# Backend API base URL
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Local dev tenant override — set to your university subdomain
# Django will use this header to resolve the tenant without needing real DNS
VITE_TENANT_SUBDOMAIN=sit
